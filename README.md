# Codespaces Dotfiles Template

jose

This repo is a starting point for using custom dotfiles (terminal / editor configuration) with GitHub Codespaces

## How to use this repo

In order to use this template, 

1. [create a new repo from this template](https://github.com/cwndrws/codespaces-dotfiles-template/generate). Make it public and name it `dotfiles`.
1. Edit the files that you want to customize in `tmux.conf`, `vimrc`, etc.
1. Push your changes to your `dotfiles` repo
1. Create a new codespace, and see your new dotfiles
1. 🍾

## What's in here?

Just some blank configuration files for popular Text User Interface (TUI) apps that folks may want to use for development in codespaces and a script to put them in the right place.

This supports the following tools:

* Tmux
* Screen
* Vim
* Emacs

## Vim Plugins

This repo uses the wonderful [Vim Plug](https://github.com/junegunn/vim-plug) tool to manage vim plugins. If you'd like to add plugins, simply add them between the opening and closing plug statements in the `vimrc` file.

## Jokes

*Three jokes about this repo, reviewed and generated by our dedicated joker agents:*

**1. The Emacs Support Joke**

This repo *technically* supports Emacs the same way a restaurant *technically* supports vegans — by handing them a blank piece of paper. The entire `emacs` config file is exactly one line: a comment saying "Add your emacs config here." Meanwhile, the `vimrc` is a 234-line epic novel. 📜

**2. The Colorscheme Graveyard Joke**

The `vimrc` has 8+ colorschemes installed with hand-crafted `highlight` overrides — `molokai`, `darcula`, `gruvbox`, `github`, `envy`, `paper`, `iceberg`, `nord` — every single one lovingly commented out. The last one standing: `vividchalk`.

> *The real colorscheme was the friends we commented out along the way.*

**3. The Install Script Joke**

The `install.sh` carefully symlinks your beloved `vim`, `neovim`, *and* `emacs` configs — covering all bases in the Great Editor War — and then celebrates by running `rm -rf $HOME/.config` just to make room for neovim anyway.

It's the dotfiles equivalent of packing every outfit you own for a trip, then nuking your entire wardrobe so your one nightly-build Hawaiian shirt has somewhere to hang. 🌺

> *Why did the dotfiles installer join a support group? Because every time it made a new friend (symlink), it had to destroy something first.* 🗑️
